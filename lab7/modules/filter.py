import numpy as np
from delay import Delay

class Filter():

    __init__(self):
        self.a1
        self.a2
        self.b1
        self.b2
        self.gain = 1
        self.d = Delay(10,2)


    def filtering(self, buffer):
        result = np.zeros(buffer)
        for i in len(buffer)
        self.gain(1+self.a1*)/()
        return result 